<h1>Managed resources</h1>

<p class="l-ui-utility--mt-md">Managed resources give your Rails models a convention-over-configuration index view with search, sorting, and pagination. Include the concern, define your columns, and add a route - the engine handles the rest.</p>

<h2 class="l-ui-utility--mt-2xl">Setup</h2>

<h3 class="l-ui-utility--mt-xl">1. Include the concern</h3>

<p class="l-ui-utility--mt-md">Add <code>Layered::Ui::ManagedResource</code> to any model you want to manage. Override the class methods to configure which columns, search fields, and sort order to use:</p>

<pre class="l-ui-surface l-ui-utility--mt-md"><code>class Post &lt; ApplicationRecord
  include Layered::Ui::ManagedResource

  belongs_to :user

  def self.l_ui_managed_columns
    [
      { attribute: :title, primary: true },
      { attribute: :body },
      { attribute: :created_at, label: "Created" }
    ]
  end

  def self.l_ui_managed_search_fields
    [:title, :body]
  end

  def self.l_ui_managed_default_sort
    { attribute: :created_at, direction: :desc }
  end
end</code></pre>

<h3 class="l-ui-utility--mt-xl">2. Add a route</h3>

<p class="l-ui-utility--mt-md">Register the model in your routes file:</p>

<pre class="l-ui-surface l-ui-utility--mt-md"><code>l_ui_managed_resources :posts</code></pre>

<p class="l-ui-utility--mt-md">This generates <code>GET /posts</code> pointing to a shared controller that reads the model's configuration and renders the index view. The named route helper is <code>managed_posts_path</code>.</p>

<p class="l-ui-utility--mt-md">To specify a different model class:</p>

<pre class="l-ui-surface l-ui-utility--mt-md"><code>l_ui_managed_resources :articles, model: "BlogPost"</code></pre>

<p class="l-ui-utility--mt-md">Routes work inside scopes:</p>

<pre class="l-ui-surface l-ui-utility--mt-md"><code>scope "/admin" do
  l_ui_managed_resources :posts
end
# Generates GET /admin/posts with helper managed_admin_posts_path</code></pre>

<p class="l-ui-utility--mt-md">That's it. No controller to write, no view to create.</p>

<h2 class="l-ui-utility--mt-2xl">Configuration</h2>

<p class="l-ui-utility--mt-md">All configuration is done via class methods on the model. Each has a sensible default that you can override:</p>

<div class="l-ui-container--table l-ui-utility--mt-lg">
  <table class="l-ui-table">
    <caption class="l-ui-sr-only">Configuration methods</caption>
    <thead class="l-ui-table__header">
      <tr>
        <th class="l-ui-table__header-cell" scope="col">Method</th>
        <th class="l-ui-table__header-cell" scope="col">Default</th>
        <th class="l-ui-table__header-cell" scope="col">Purpose</th>
      </tr>
    </thead>
    <tbody class="l-ui-table__body">
      <tr>
        <th class="l-ui-table__cell--primary" scope="row"><code>l_ui_managed_columns</code></th>
        <td class="l-ui-table__cell"><code>[{ attribute: :id }]</code></td>
        <td class="l-ui-table__cell">Columns to display. Only whitelisted attributes are shown.</td>
      </tr>
      <tr>
        <th class="l-ui-table__cell--primary" scope="row"><code>l_ui_managed_search_fields</code></th>
        <td class="l-ui-table__cell"><code>[]</code></td>
        <td class="l-ui-table__cell">Fields to search via Ransack. Empty disables search.</td>
      </tr>
      <tr>
        <th class="l-ui-table__cell--primary" scope="row"><code>l_ui_managed_default_sort</code></th>
        <td class="l-ui-table__cell"><code>{ attribute: :id, direction: :desc }</code></td>
        <td class="l-ui-table__cell">Default sort when no sort is active.</td>
      </tr>
      <tr>
        <th class="l-ui-table__cell--primary" scope="row"><code>l_ui_managed_per_page</code></th>
        <td class="l-ui-table__cell"><code>20</code></td>
        <td class="l-ui-table__cell">Records per page.</td>
      </tr>
    </tbody>
  </table>
</div>

<h3 class="l-ui-utility--mt-xl">Column options</h3>

<p class="l-ui-utility--mt-md">Each column hash supports:</p>

<div class="l-ui-container--table l-ui-utility--mt-lg">
  <table class="l-ui-table">
    <caption class="l-ui-sr-only">Column options</caption>
    <thead class="l-ui-table__header">
      <tr>
        <th class="l-ui-table__header-cell" scope="col">Key</th>
        <th class="l-ui-table__header-cell" scope="col">Default</th>
        <th class="l-ui-table__header-cell" scope="col">Description</th>
      </tr>
    </thead>
    <tbody class="l-ui-table__body">
      <tr>
        <th class="l-ui-table__cell--primary" scope="row"><code>attribute</code></th>
        <td class="l-ui-table__cell">Required</td>
        <td class="l-ui-table__cell">The model attribute to display.</td>
      </tr>
      <tr>
        <th class="l-ui-table__cell--primary" scope="row"><code>label</code></th>
        <td class="l-ui-table__cell">Derived from attribute</td>
        <td class="l-ui-table__cell">Column header text. Auto-generated as sentence case if omitted.</td>
      </tr>
      <tr>
        <th class="l-ui-table__cell--primary" scope="row"><code>sortable</code></th>
        <td class="l-ui-table__cell"><code>true</code></td>
        <td class="l-ui-table__cell">Whether the column header is a sort link.</td>
      </tr>
      <tr>
        <th class="l-ui-table__cell--primary" scope="row"><code>primary</code></th>
        <td class="l-ui-table__cell">First column</td>
        <td class="l-ui-table__cell">Renders as <code>&lt;th scope="row"&gt;</code> for accessibility.</td>
      </tr>
    </tbody>
  </table>
</div>

<h2 class="l-ui-utility--mt-2xl">Authentication</h2>

<p class="l-ui-utility--mt-md">The managed resource controller inherits from your application's <code>ApplicationController</code>, so any global <code>before_action</code> filters (such as Devise's <code>authenticate_user!</code>) apply automatically.</p>

<p class="l-ui-utility--mt-md">If your app authenticates per-controller rather than globally, configure a before action in an initialiser:</p>

<pre class="l-ui-surface l-ui-utility--mt-md"><code># config/initializers/layered_ui.rb
Layered::Ui.l_ui_managed_before_action = :authenticate_user!</code></pre>

<h2 class="l-ui-utility--mt-2xl">How it works</h2>

<p class="l-ui-utility--mt-md">The concern is secure by default - only <code>:id</code> is shown unless you explicitly whitelist columns. Ransack's <code>ransackable_attributes</code> and <code>ransackable_associations</code> are automatically derived from your column and search field definitions, so you don't need to set them manually. If your model already defines its own <code>ransackable_attributes</code> or <code>ransackable_associations</code>, those take precedence - the concern only provides defaults.</p>

<p class="l-ui-utility--mt-md">Managed resources require the <code>ransack</code> and <code>pagy</code> gems. Add both to your Gemfile before including the concern.</p>

<h2 class="l-ui-utility--mt-2xl">Live example</h2>

<p class="l-ui-utility--mt-md">The <%= link_to "Posts", managed_posts_path %> page is a managed resource using the configuration shown above. Try searching, sorting by column headers, and paginating.</p>

<h2 class="l-ui-utility--mt-2xl">Standalone table helper</h2>

<p class="l-ui-utility--mt-md">Use <code>l_ui_managed_table</code> to render a styled table in any view. This is useful when you need your own controller for custom scoping, routing, or header actions, but still want the engine's table styles.</p>

<pre class="l-ui-surface l-ui-utility--mt-md"><code>l_ui_managed_table(@personas,
  columns: [
    { attribute: :name, primary: true, render: ->(r) { link_to r.name, persona_path(r) } },
    { attribute: :description, render: ->(r) { truncate(r.description, length: 60) } },
  ],
  actions: ->(r) { link_to "Edit", edit_persona_path(r) },
  caption: "Personas"
)</code></pre>

<h3 class="l-ui-utility--mt-xl">Parameters</h3>

<div class="l-ui-container--table l-ui-utility--mt-lg">
  <table class="l-ui-table">
    <caption class="l-ui-sr-only">Helper parameters</caption>
    <thead class="l-ui-table__header">
      <tr>
        <th class="l-ui-table__header-cell" scope="col">Parameter</th>
        <th class="l-ui-table__header-cell" scope="col">Default</th>
        <th class="l-ui-table__header-cell" scope="col">Description</th>
      </tr>
    </thead>
    <tbody class="l-ui-table__body">
      <tr>
        <th class="l-ui-table__cell--primary" scope="row"><code>records</code></th>
        <td class="l-ui-table__cell">Required</td>
        <td class="l-ui-table__cell">An enumerable of model objects.</td>
      </tr>
      <tr>
        <th class="l-ui-table__cell--primary" scope="row"><code>columns:</code></th>
        <td class="l-ui-table__cell">Required</td>
        <td class="l-ui-table__cell">Array of column hashes. See column options below.</td>
      </tr>
      <tr>
        <th class="l-ui-table__cell--primary" scope="row"><code>caption:</code></th>
        <td class="l-ui-table__cell"><code>nil</code></td>
        <td class="l-ui-table__cell">Table caption (rendered as screen-reader-only text).</td>
      </tr>
      <tr>
        <th class="l-ui-table__cell--primary" scope="row"><code>actions:</code></th>
        <td class="l-ui-table__cell"><code>nil</code></td>
        <td class="l-ui-table__cell">Proc receiving a record, returns HTML for a right-aligned actions cell.</td>
      </tr>
      <tr>
        <th class="l-ui-table__cell--primary" scope="row"><code>actions_label:</code></th>
        <td class="l-ui-table__cell"><code>"Actions"</code></td>
        <td class="l-ui-table__cell">Header text for the actions column.</td>
      </tr>
      <tr>
        <th class="l-ui-table__cell--primary" scope="row"><code>query:</code></th>
        <td class="l-ui-table__cell"><code>nil</code></td>
        <td class="l-ui-table__cell">Ransack search object. When provided, sortable columns render as sort links.</td>
      </tr>
      <tr>
        <th class="l-ui-table__cell--primary" scope="row"><code>turbo_frame:</code></th>
        <td class="l-ui-table__cell"><code>nil</code></td>
        <td class="l-ui-table__cell">Turbo frame name for sort links. Used with <code>query:</code>.</td>
      </tr>
    </tbody>
  </table>
</div>

<h3 class="l-ui-utility--mt-xl">Column options</h3>

<div class="l-ui-container--table l-ui-utility--mt-lg">
  <table class="l-ui-table">
    <caption class="l-ui-sr-only">Column options for standalone helper</caption>
    <thead class="l-ui-table__header">
      <tr>
        <th class="l-ui-table__header-cell" scope="col">Key</th>
        <th class="l-ui-table__header-cell" scope="col">Default</th>
        <th class="l-ui-table__header-cell" scope="col">Description</th>
      </tr>
    </thead>
    <tbody class="l-ui-table__body">
      <tr>
        <th class="l-ui-table__cell--primary" scope="row"><code>attribute</code></th>
        <td class="l-ui-table__cell">Required</td>
        <td class="l-ui-table__cell">The model attribute. Used for data extraction and label generation.</td>
      </tr>
      <tr>
        <th class="l-ui-table__cell--primary" scope="row"><code>label</code></th>
        <td class="l-ui-table__cell">Derived from attribute</td>
        <td class="l-ui-table__cell">Custom header text.</td>
      </tr>
      <tr>
        <th class="l-ui-table__cell--primary" scope="row"><code>primary</code></th>
        <td class="l-ui-table__cell">First column</td>
        <td class="l-ui-table__cell">Renders as <code>&lt;th scope="row"&gt;</code> for accessibility.</td>
      </tr>
      <tr>
        <th class="l-ui-table__cell--primary" scope="row"><code>sortable</code></th>
        <td class="l-ui-table__cell"><code>true</code></td>
        <td class="l-ui-table__cell">Whether to show a sort link when <code>query:</code> is provided.</td>
      </tr>
      <tr>
        <th class="l-ui-table__cell--primary" scope="row"><code>render</code></th>
        <td class="l-ui-table__cell"><code>nil</code></td>
        <td class="l-ui-table__cell">Proc receiving a record, returns cell content. Falls back to <code>record.public_send(attribute)</code> with automatic date formatting.</td>
      </tr>
    </tbody>
  </table>
</div>

<h3 class="l-ui-utility--mt-xl">Example</h3>

<%= l_ui_managed_table(@posts_for_table,
      columns: [
        { attribute: :title, primary: true },
        { attribute: :body, render: ->(r) { truncate(r.body, length: 40) } },
        { attribute: :created_at, label: "Created" },
      ],
      actions: ->(r) { link_to "View", "#" },
      caption: "Posts (standalone)"
    ) %>
